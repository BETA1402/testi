/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/my-next-app', // Replace with your repository name
    output: 'export',
    // Set assetPrefix to handle static assets correctly
    assetPrefix: '/my-next-app/', // R};
}
export default nextConfig;
